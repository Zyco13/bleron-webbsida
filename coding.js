//const monitor = document.querySelector('.monitor-screen');
const osOverlay = document.getElementById('osOverlay');
const osClose = document.getElementById('osClose');

// Klicka på monitorn → öppna OS
/*monitor.addEventListener('click', function() {
    osOverlay.classList.add('active');
});

// Klicka på X → stäng OS
osClose.addEventListener('click', function() {
    osOverlay.classList.remove('active');
});

const iconCV = document.getElementById('iconCV');
const iconPortfolio = document.getElementById('iconPortfolio');
const iconBilder = document.getElementById('iconBilder');

// Klicka på CV → scrollar till CV-sektionen
iconCV.addEventListener('click', function() {
    osOverlay.classList.remove('active');
    document.getElementById('cv').scrollIntoView({ behavior: 'smooth' });
});

// Klicka på Portfolio → scrollar till portfolio-sektionen
iconPortfolio.addEventListener('click', function() {
    osOverlay.classList.remove('active');
    document.getElementById('portfolio').scrollIntoView({ behavior: 'smooth' });
});

// Klicka på Bilder → visar ett meddelande för nu
iconBilder.addEventListener('click', function() {
    alert('Bilder kommer snart!');
}); */